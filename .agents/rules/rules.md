---
trigger: always_on
---

do not run dangerous database command, that will wipe the database, even if this is staging only and not live - unless i told you to run it. Do not run browser test ever unless I told you to run it. Make sure that the design looks consistent. Do not use emojis when designing. If you need to use an icon, stick on using the fontawesome.